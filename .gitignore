# Artifact sinh ra khi chạy scripts/pi-setup-backup.sh (không track trong git)
pi-setup-portable.tar.gz
pi-setup-portable.tar.gz.tmp.*

# Log cài đặt của restore
pi-setup-install.log

# Rác hệ điều hành
.DS_Store

# Thư mục font người dùng tự đặt vào (không phải nội dung setup)
fonts/
